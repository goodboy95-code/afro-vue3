export class RoleDepartment {
    roleId: number;
    departmentId: number;

    constructor(roleId: number, departmentId: number) {
        this.roleId = roleId;
        this.departmentId = departmentId;
    }
}